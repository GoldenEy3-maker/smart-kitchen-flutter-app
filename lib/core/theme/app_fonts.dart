class AppFonts {
  static const String inter = "Inter";
  static const String manrope = "Manrope";

  /// Monochrome (outline) emoji glyphs, unlike the platform's default color
  /// emoji font. This lets [TextStyle.color] actually tint the glyph so
  /// emoji can match the single-tone icon look used in the design.
  static const String notoEmoji = "NotoEmoji";
}
